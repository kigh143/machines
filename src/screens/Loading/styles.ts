import { Dimensions, StyleSheet } from "react-native";

export default StyleSheet.create({
  page: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
  },
  textCenter: {
    textAlign: "center",
  },
  heading: {
    fontWeight: "bold",
    fontSize: 17,
    marginBottom:7
  },
});
